# Organization and project keys are displayed in the right sidebar of the project homepage
sonar.organization=monpm
sonar.projectKey=first-test_python-DOTT
sonar.login=a71c89946f6b288800ef515e37e02b583d3df596
sonar.host.url=https://sonarcloud.io

# --- optional properties ---

# defaults to project key
sonar.projectName=python-DOTT
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .

#language
sonar.language= py

#sources
#sonar.sources=.
## marca un error sobre "sonar.java.binaries" en la documentación de sonar viene esta regla para excluir sources 
sonar.exclusions= **/*.java

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# unit test (hopefully)
sonar.python.xunit.reportPath= xmlReport/*.xml

